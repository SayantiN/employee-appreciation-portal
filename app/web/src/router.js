// The live app uses clean URLs. The static design demo swaps this file for a
// HashRouter, because GitHub Pages cannot rewrite unknown paths to index.html.
export { BrowserRouter as Router } from 'react-router-dom';
